# DEPRECATED — see docker-compose.yml (Plan #13).
#
# Plan #12 created this file to hold the mail-stack service block before the full
# production compose existed. Plan #13 assembled the canonical production stack
# (app, worker, postgres, minio, caddy, glitchtip + the mail services) in
# docker-compose.yml and MERGED this file's services into it.
#
# `docker compose up -d` (no -f) now reads the canonical docker-compose.yml.
# This file is retained only as an alias so any existing operator muscle-memory
# of `-f docker-compose.prod.yml` still resolves to the production stack.
#
# Do NOT add services here — edit docker-compose.yml instead.

include:
  - docker-compose.yml
