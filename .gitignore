# env / secrets
.env
.env.*
!.env.example

# DKIM private keys (Plan #12) — generated per-host, never committed
docker/opendkim/keys/

# node / next
node_modules/
.next/
out/
dist/
.turbo/
.pnpm-store/

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo

# local data / backups (never commit volumes or dumps)
data/
backups/
postgres-data/
minio-data/

# coverage / tests
coverage/
.nyc_output/
test-results/
playwright-report/
playwright/.cache/

# typescript build info
*.tsbuildinfo

# vercel (only if ever used)
.vercel

# brainstorming session artifacts (visual companion)
.superpowers/

# user-level claude harness state
.claude/

# Playwright MCP demo artifacts
.playwright-mcp/
demo-*.png
